apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: hello-world-1
  namespace: kube-system
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - host: hello-world-1.lonelyplanet-digital.sandbox.redventures.io
    http:
      paths:
      - backend:
          serviceName: hello-world-1
          servicePort: 80
