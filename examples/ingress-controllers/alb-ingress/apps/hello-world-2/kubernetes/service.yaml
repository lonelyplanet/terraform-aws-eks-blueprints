apiVersion: v1
kind: Service
metadata:
  name: hello-world-2
  annotations:
    external-dns.alpha.kubernetes.io/internal-hostname: hello-world-2.lonelyplanet-digital.sandbox.redventures.io
  labels:
    lp-service-id: hello-world-2
spec:
  ports:
  - name: http
    port: 3000
    targetPort: 3000
  selector:
    lp-service-id: hello-world-2
  type: NodePort
